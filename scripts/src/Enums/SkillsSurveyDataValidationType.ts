/**
 * Skills Survey Data Validation Type - Each value represents a unique type of validation
 * against the Skills database.
 */
export enum SkillsSurveyDataValidationType {
    // All new values be listed in alphabetical order
    HasManufacturingData = "HasManufacturingData",
    HasSourcingData = "HasSourcingData",
}
